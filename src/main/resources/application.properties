# GraphHopper API key (set via env var in production)
graphhopper.api.key=0d2727ea-bb2d-4d3e-a9b3-3429c320f16a
# For local dev, use in-memory HSQL to avoid file lock/creation issues
main.datasource.url=jdbc:hsqldb:mem:donilogistics
main.datasource.username=sa
main.datasource.password=

main.liquibase.change-log=com/donilogistics/liquibase/changelog.xml


jmix.core.available-locales=en

# REST endpoints require authentication
jmix.resource-server.authenticated-url-patterns=/rest/**

# Consider disabling anonymous access to the docs in production environment by removing this property
jmix.resource-server.anonymous-url-patterns=/rest/docs/*,/api/auth/**,/rest/services/doni_UserRegistrationService/getActiveOrganizations,/rest/services/doni_UserRegistrationService/registerUser,/rest/services/doni_UserRegistrationService/verifyEmail

# 'debug' level logs SQL generated by EclipseLink ORM
logging.level.eclipselink.logging.sql=info

# 'debug' level logs data store operations
logging.level.io.jmix.core.datastore=info

# 'debug' level logs access control constraints
logging.level.io.jmix.core.AccessLogger=debug

# 'debug' level logs all Jmix debug output
logging.level.io.jmix=info

# 'debug' or 'trace' levels help to troubleshoot security problems
logging.level.org.springframework.security=info

# The client id
spring.security.oauth2.authorizationserver.client.myclient.registration.client-id=hqdktdfktq
# The client secret
spring.security.oauth2.authorizationserver.client.myclient.registration.client-secret={noop}vpjmbwfpxv
# Enable Client Credential grant for the my-client
spring.security.oauth2.authorizationserver.client.myclient.registration.authorization-grant-types=client_credentials
# Client credentials must be passed in the Authorization header using the HTTP Basic authentication scheme
spring.security.oauth2.authorizationserver.client.myclient.registration.client-authentication_methods=client_secret_basic
# Use opaque tokens instead of JWT
spring.security.oauth2.authorizationserver.client.myclient.token.access-token-format=reference

# Define resource roles for the client
jmix.authserver.client.myclient.client-id=hqdktdfktq
jmix.authserver.client.myclient.resource-roles=rest-minimal,logistics-operations

# Outbound mail (configure with your cPanel SMTP)
spring.mail.host=mail.donilogistics.com
spring.mail.port=465
spring.mail.username=verify@donilogistics.com
spring.mail.password=Solo@v5010
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.starttls.enable=false
spring.mail.properties.mail.smtp.ssl.trust=mail.donilogistics.com

# App mail and public URL
app.mail.from=verify@donilogistics.com
app.public-url=http://localhost:3000

# SMTP test and debug (temporary)
spring.mail.test-connection=true
spring.mail.properties.mail.debug=true

# REST services configuration
jmix.rest.services-config=rest-services.xml

# (merged above)